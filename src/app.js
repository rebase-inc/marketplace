var React = require('react');
var ReactDOM = require('react-dom');
var RebaseApp = require('./components/RebaseApp.react');
var D3 = require('d3');

ReactDOM.render(<RebaseApp />, document.getElementById('react-app'));
